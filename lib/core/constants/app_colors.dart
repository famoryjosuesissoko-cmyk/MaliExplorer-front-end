import 'package:flutter/material.dart';

class AppColors {
  // Verts
  static const Color primaryForest = Color(0xFF075E4D);    // Vert Forêt Profond (Vert principal)
  static const Color secondaryEmerald = Color(0xFF0E8F76); // Vert Émeraude (Vert secondaire)

  // Or & Jaune (Patrimoine & Accents)
  static const Color sahelGold = Color(0xFFD6A23A);         // Or Sahélien (Or / patrimoine)
  static const Color solarYellow = Color(0xFFF2B544);       // Jaune Solaire (Accent jaune)

  // Arrière-plans & Surfaces
  static const Color mistIvory = Color(0xFFF7F8F5);         // Ivoire Brumeux (Plan arrière)
  static const Color pureWhite = Color(0xFFFFFFFF);          // Blanc Pur (Surface / cartes)

  // Textes
  static const Color textPrimary = Color(0xFF16332D);        // Vert Anthracite (Texte principal)
  static const Color textSecondary = Color(0xFF6C7C77);      // Gris Sauge (Texte secondaire)
}